@extends('layouts.main')

@section('title', 'Évaluations - Attribution ' . $attribution->numero_attribution)

@section('breadcrumb')
    <a href="{{ route('evaluations.index') }}" class="text-white/80 hover:text-white transition-colors">Évaluations</a>
    <i class="fas fa-chevron-right text-white/50 text-xs mx-2"></i>
    <span class="text-white font-medium">Attribution {{ $attribution->numero_attribution }}</span>
@endsection

@section('content')
    <!-- Header -->
    <div class="bg-gradient-to-r from-gray-50 to-white border-b border-gray-200 shadow-sm">
        <div class="px-3 sm:px-4 lg:px-6 py-4">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
                <div class="flex items-center space-x-4">
                    <a href="{{ route('attributions.show', $attribution->id_attribution) }}"
                        class="p-2 hover:bg-gray-100 rounded-lg transition-all duration-200">
                        <i class="fas fa-arrow-left text-gray-600"></i>
                    </a>
                    <div>
                        <h1 class="text-2xl font-bold text-gray-800">Évaluations de l'attribution</h1>
                        <p class="text-gray-600 mt-1">
                            {{ $attribution->numero_attribution }} -
                            Lot {{ $attribution->lot->numero ?? 'N/A' }}
                        </p>
                    </div>
                </div>

                <!-- Actions -->
                <div class="flex items-center space-x-2">
                    @if(!$evaluationCourante || $evaluationCourante->isRejetee())
                        <a href="{{ route('evaluations.create', $attribution->id_attribution) }}"
                            class="px-4 py-2.5 bg-gradient-to-r from-orange-500 to-orange-600 hover:from-orange-600 hover:to-orange-700 text-white rounded-lg transition-all duration-200 flex items-center space-x-2 shadow-md">
                            <i class="fas fa-plus text-sm"></i>
                            <span class="text-sm font-medium">Nouvelle évaluation</span>
                        </a>
                    @endif
                </div>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <main class="flex-1 overflow-y-auto bg-gradient-to-br from-gray-50 to-gray-100 p-3 sm:p-4 lg:p-6">

        @include('partials.alerts')

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
            <!-- Colonne principale -->
            <div class="lg:col-span-2 space-y-6">

                <!-- Évaluation courante -->
                @if($evaluationCourante)
                    <div class="bg-white rounded-2xl shadow-lg overflow-hidden">
                        <div class="px-6 py-4 bg-gradient-to-r from-indigo-50 to-white border-b border-gray-200">
                            <div class="flex items-center justify-between">
                                <h2 class="text-lg font-bold text-gray-800 flex items-center">
                                    <i class="fas fa-clipboard-check text-indigo-500 mr-2"></i>
                                    Évaluation en cours
                                </h2>
                                <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-semibold {{ $evaluationCourante->statut_badge_class }}">
                                    <i class="fas fa-{{ $evaluationCourante->statut_icon }} mr-1"></i>
                                    {{ $evaluationCourante->statut_label }}
                                </span>
                            </div>
                        </div>
                        <div class="p-6">
                            <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
                                <div>
                                    <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Numéro</label>
                                    <p class="text-gray-900 font-medium">{{ $evaluationCourante->numero_evaluation }}</p>
                                </div>
                                <div>
                                    <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Version</label>
                                    <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-purple-100 text-purple-700">
                                        V{{ $evaluationCourante->version }}
                                    </span>
                                </div>
                                <div>
                                    <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Date</label>
                                    <p class="text-gray-900">
                                        {{ $evaluationCourante->date_evaluation ? $evaluationCourante->date_evaluation->format('d/m/Y H:i') : 'Non définie' }}
                                    </p>
                                </div>
                            </div>

                            <!-- Barre de progression -->
                            <div class="mb-6">
                                <div class="flex items-center justify-between mb-2">
                                    <span class="text-sm font-semibold text-gray-700">Score</span>
                                    <span class="text-2xl font-bold {{ $evaluationCourante->pourcentage_final >= 70 ? 'text-green-600' : ($evaluationCourante->pourcentage_final >= 50 ? 'text-yellow-600' : 'text-red-600') }}">
                                        {{ number_format($evaluationCourante->pourcentage_final, 1) }}%
                                    </span>
                                </div>
                                <div class="w-full bg-gray-200 rounded-full h-4">
                                    <div class="h-4 rounded-full transition-all duration-500 {{ $evaluationCourante->pourcentage_final >= 70 ? 'bg-green-500' : ($evaluationCourante->pourcentage_final >= 50 ? 'bg-yellow-500' : 'bg-red-500') }}"
                                         style="width: {{ min($evaluationCourante->pourcentage_final, 100) }}%"></div>
                                </div>
                                <div class="flex justify-between mt-2 text-sm text-gray-500">
                                    <span>{{ number_format($evaluationCourante->resultat_evaluation, 1) }} pts obtenus</span>
                                    <span>{{ number_format($evaluationCourante->note_maximale, 1) }} pts maximum</span>
                                </div>
                            </div>

                            <!-- Actions -->
                            <div class="flex items-center justify-between pt-4 border-t">
                                <a href="{{ route('evaluations.show', $evaluationCourante->id_evaluation) }}"
                                    class="px-4 py-2 bg-indigo-100 hover:bg-indigo-200 text-indigo-700 font-medium rounded-lg transition-all">
                                    <i class="fas fa-eye mr-2"></i>Voir les détails
                                </a>
                                <div class="flex items-center space-x-2">
                                    @if($evaluationCourante->peutEtreModifiee())
                                        <a href="{{ route('evaluations.edit', $evaluationCourante->id_evaluation) }}"
                                            class="px-4 py-2 bg-orange-100 hover:bg-orange-200 text-orange-700 font-medium rounded-lg transition-all">
                                            <i class="fas fa-edit mr-2"></i>Modifier
                                        </a>
                                    @endif
                                    @if($evaluationCourante->peutEtreTerminee())
                                        <form action="{{ route('evaluations.terminer', $evaluationCourante->id_evaluation) }}" method="POST" class="inline">
                                            @csrf
                                            <button type="submit" class="px-4 py-2 bg-green-100 hover:bg-green-200 text-green-700 font-medium rounded-lg transition-all">
                                                <i class="fas fa-check mr-2"></i>Terminer
                                            </button>
                                        </form>
                                    @endif
                                </div>
                            </div>
                        </div>
                    </div>
                @else
                    <!-- Pas d'évaluation -->
                    <div class="bg-white rounded-2xl shadow-lg overflow-hidden">
                        <div class="p-12 text-center">
                            <i class="fas fa-clipboard-list text-6xl text-gray-300 mb-4"></i>
                            <h3 class="text-xl font-semibold text-gray-600 mb-2">Aucune évaluation</h3>
                            <p class="text-gray-500 mb-6">Aucune évaluation n'a encore été créée pour cette attribution.</p>
                            <a href="{{ route('evaluations.create', $attribution->id_attribution) }}"
                                class="inline-flex items-center px-6 py-3 bg-gradient-to-r from-orange-500 to-orange-600 hover:from-orange-600 hover:to-orange-700 text-white font-medium rounded-lg transition-all shadow-md">
                                <i class="fas fa-plus mr-2"></i>Créer une évaluation
                            </a>
                        </div>
                    </div>
                @endif

                <!-- Historique des versions -->
                @if($evaluations->count() > 1)
                    <div class="bg-white rounded-2xl shadow-lg overflow-hidden">
                        <div class="px-6 py-4 bg-gradient-to-r from-purple-50 to-white border-b border-gray-200">
                            <h2 class="text-lg font-bold text-gray-800 flex items-center">
                                <i class="fas fa-history text-purple-500 mr-2"></i>
                                Historique des versions
                                <span class="ml-2 px-2 py-1 text-xs font-medium bg-purple-100 text-purple-700 rounded-full">
                                    {{ $evaluations->count() }}
                                </span>
                            </h2>
                        </div>
                        <div class="divide-y divide-gray-200">
                            @foreach($evaluations as $eval)
                                <div class="p-4 hover:bg-gray-50 transition-colors {{ $eval->is_current ? 'bg-purple-50' : '' }}">
                                    <div class="flex items-center justify-between">
                                        <div class="flex items-center space-x-3">
                                            <span class="inline-flex items-center justify-center w-10 h-10 rounded-full {{ $eval->is_current ? 'bg-purple-500 text-white' : 'bg-gray-200 text-gray-600' }} font-bold">
                                                V{{ $eval->version }}
                                            </span>
                                            <div>
                                                <div class="flex items-center space-x-2">
                                                    <span class="font-medium text-gray-900">{{ $eval->numero_evaluation }}</span>
                                                    @if($eval->is_current)
                                                        <span class="px-2 py-0.5 text-xs font-medium bg-green-100 text-green-700 rounded-full">Active</span>
                                                    @endif
                                                </div>
                                                <p class="text-sm text-gray-500">
                                                    {{ $eval->created_at->format('d/m/Y H:i') }}
                                                    @if($eval->creator)
                                                        par {{ $eval->creator->name }}
                                                    @endif
                                                </p>
                                            </div>
                                        </div>
                                        <div class="flex items-center space-x-4">
                                            <div class="text-right">
                                                <span class="text-lg font-bold {{ $eval->pourcentage_final >= 70 ? 'text-green-600' : ($eval->pourcentage_final >= 50 ? 'text-yellow-600' : 'text-red-600') }}">
                                                    {{ number_format($eval->pourcentage_final, 1) }}%
                                                </span>
                                                <span class="inline-flex items-center px-2 py-1 rounded text-xs font-medium {{ $eval->statut_badge_class }}">
                                                    {{ $eval->statut_label }}
                                                </span>
                                            </div>
                                            <a href="{{ route('evaluations.show', $eval->id_evaluation) }}"
                                                class="p-2 text-gray-400 hover:text-indigo-600 hover:bg-indigo-50 rounded-lg transition-colors">
                                                <i class="fas fa-eye"></i>
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            @endforeach
                        </div>
                    </div>
                @endif

            </div>

            <!-- Colonne latérale -->
            <div class="space-y-6">

                <!-- Informations attribution -->
                <div class="bg-white rounded-2xl shadow-lg overflow-hidden">
                    <div class="px-6 py-4 bg-gradient-to-r from-orange-50 to-white border-b border-gray-200">
                        <h2 class="text-lg font-bold text-gray-800 flex items-center">
                            <i class="fas fa-file-contract text-orange-500 mr-2"></i>
                            Attribution
                        </h2>
                    </div>
                    <div class="p-6 space-y-4">
                        <div>
                            <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Numéro</label>
                            <a href="{{ route('attributions.show', $attribution->id_attribution) }}"
                                class="text-orange-600 hover:text-orange-800 font-medium">
                                {{ $attribution->numero_attribution }}
                            </a>
                        </div>
                        <div>
                            <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Statut</label>
                            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-semibold {{ $attribution->statut_badge_class }}">
                                {{ $attribution->statut_label }}
                            </span>
                        </div>
                        <div>
                            <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Date d'attribution</label>
                            <p class="text-gray-900">
                                {{ $attribution->date_attribution ? $attribution->date_attribution->format('d/m/Y') : 'Non définie' }}
                            </p>
                        </div>
                    </div>
                </div>

                <!-- Informations lot -->
                <div class="bg-white rounded-2xl shadow-lg overflow-hidden">
                    <div class="px-6 py-4 bg-gradient-to-r from-indigo-50 to-white border-b border-gray-200">
                        <h2 class="text-lg font-bold text-gray-800 flex items-center">
                            <i class="fas fa-box text-indigo-500 mr-2"></i>
                            Lot
                        </h2>
                    </div>
                    <div class="p-6 space-y-4">
                        <div>
                            <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Numéro</label>
                            <span class="inline-flex items-center px-3 py-1 rounded-lg text-sm font-bold bg-indigo-100 text-indigo-700">
                                {{ $attribution->lot->numero ?? 'N/A' }}
                            </span>
                        </div>
                        <div>
                            <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Libellé</label>
                            <p class="text-gray-900">{{ $attribution->lot->libelle ?? 'N/A' }}</p>
                        </div>
                        <div>
                            <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Appel d'offres</label>
                            <p class="text-gray-900">{{ $attribution->lot->appelOffre->numero_appel_offre ?? 'N/A' }}</p>
                        </div>
                    </div>
                </div>

                <!-- Informations prestataire -->
                <div class="bg-white rounded-2xl shadow-lg overflow-hidden">
                    <div class="px-6 py-4 bg-gradient-to-r from-green-50 to-white border-b border-gray-200">
                        <h2 class="text-lg font-bold text-gray-800 flex items-center">
                            <i class="fas fa-building text-green-500 mr-2"></i>
                            Prestataire
                        </h2>
                    </div>
                    <div class="p-6 space-y-4">
                        <div>
                            <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Raison sociale</label>
                            <p class="text-gray-900 font-medium">{{ $attribution->prestataire->raison_sociale_prestataire ?? 'N/A' }}</p>
                        </div>
                        <div>
                            <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Contact</label>
                            <p class="text-gray-600 text-sm">{{ $attribution->prestataire->email_prestataire ?? '' }}</p>
                            <p class="text-gray-600 text-sm">{{ $attribution->prestataire->telephone_principal_prestataire ?? '' }}</p>
                        </div>
                    </div>
                </div>

                <!-- Critères d'évaluation -->
                <div class="bg-white rounded-2xl shadow-lg overflow-hidden">
                    <div class="px-6 py-4 bg-gradient-to-r from-blue-50 to-white border-b border-gray-200">
                        <div class="flex items-center justify-between">
                            <h2 class="text-lg font-bold text-gray-800 flex items-center">
                                <i class="fas fa-list-check text-blue-500 mr-2"></i>
                                Critères
                            </h2>
                            <span class="px-2 py-1 text-xs font-medium bg-blue-100 text-blue-700 rounded-full">
                                {{ $criteres->count() }}
                            </span>
                        </div>
                    </div>
                    <div class="divide-y divide-gray-200 max-h-64 overflow-y-auto">
                        @forelse($criteres as $critere)
                            <div class="p-3 hover:bg-gray-50">
                                <div class="flex items-center justify-between">
                                    <div>
                                        <span class="text-xs text-gray-500">{{ $critere->numero_critere_evaluation }}</span>
                                        <p class="text-sm font-medium text-gray-900">{{ Str::limit($critere->libelle_critere_evaluation, 30) }}</p>
                                    </div>
                                    <span class="text-sm font-bold text-blue-600">
                                        {{ $critere->note_reference_critere_evaluation }} pts
                                    </span>
                                </div>
                            </div>
                        @empty
                            <div class="p-4 text-center text-gray-500">
                                Aucun critère défini
                            </div>
                        @endforelse
                    </div>
                    <div class="px-6 py-3 bg-blue-50 border-t">
                        <div class="flex justify-between items-center">
                            <span class="text-sm font-semibold text-gray-700">Total</span>
                            <span class="text-lg font-bold text-blue-600">{{ $totalNotesReference }} pts</span>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </main>
@endsection
